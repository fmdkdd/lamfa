{
  "name": "lambda-facets",
  "scripts": {
    "test": "mocha --ui tdd --require should --check-leaks --no-colors",
    "coverage": "istanbul cover _mocha -- --ui tdd --require should --check-leaks --no-colors"
  },
  "dependencies": {
    "optimist": "~0.6.0"
  }
}
