module Base? where


{-

Issue here is we cannot reasonably make one module disappear.  But
what should the Extension module export?  Can we export the "full
terms" and full functions imported from a Base.completed module?

-}
